# SwimMate 영상 분석 v2 — 다중 선수 카운팅 기준선

> 상태: 오프라인 실험 · 공개 API/UI 비활성
> 기준일: 2026-08-26

## 목표

대회 영상처럼 한 프레임에 여러 선수가 등장해도 선수별 추적 ID를 유지하고,
각 선수의 스트로크·완전한 사이클·발차기 이벤트를 분리해 측정한다.

기존 파이프라인은 MediaPipe `num_poses=1`과 첫 번째 포즈만 사용했으므로
다중 레인 경기를 처리할 수 없었다. v2는 다음 순서로 작동한다.

1. 수영장 ROI를 겹치는 타일로 나누어 작은 선수를 확대 검출
2. 타일 경계에서 중복 검출된 포즈 병합
3. 프레임에서 최대 10개의 포즈 선별
4. 레인 위치, 예측 중심점, 관절 형태, 바운딩 박스를 조합해 프레임 간 연결
5. 검출 순서가 바뀌어도 `S001` 같은 추적 ID 유지
6. 추적 ID별 관절 시계열 생성
7. 영법별 팔·발 이벤트 검출
8. 가시성이 부족하면 수치를 만들지 않고 분석 불가 사유 반환

## 현재 구성

| 구성 요소 | 역할 |
| --- | --- |
| `analysis_v2/types.py` | 포즈·트랙·영법 공통 데이터 계약 |
| `analysis_v2/tracking.py` | 레인 인식 다중 선수 연결 및 ID 유지 |
| `analysis_v2/counting.py` | 선수별 팔 스트로크·킥 이벤트 계산 |
| `analysis_v2/pipeline.py` | 프레임 입력부터 선수별 결과까지 조정 |
| `analysis_v2/mediapipe_provider.py` | 전체 프레임·겹침 타일 MediaPipe 다중 포즈 어댑터 |
| `analysis_v2/cli.py` | 로컬 영상 JSON 분석 명령 |
| `tests/test_multiswimmer_analysis.py` | 검출 순서 변경·가림·독립 카운팅 회귀 테스트 |

## ID 유지 원칙

대회 영상에서는 수영 선수가 같은 레인 안에서 이동하고 레인 간 교차는 거의
발생하지 않는다. 이 제약을 이용해 다음 비용이 가장 낮은 연결을 전체적으로 찾는다.

- 이전 속도로 예측한 선수 중심과 새 검출 중심의 거리
- 레인 축 위치 차이
- 주요 관절의 정규화 거리
- 바운딩 박스 겹침

선수가 잠시 물보라에 가려져도 제한 프레임 안에는 이전 트랙과 다시 연결한다.
긴 장면 전환 뒤에는 같은 사람임을 보장할 수 없으므로 새 ID를 발급한다. 얼굴
재인식은 개인정보 위험과 수영모·고글 환경의 낮은 신뢰도 때문에 사용하지 않는다.

## 카운팅 원칙

- 자유형·배영: 왼팔·오른팔 이벤트를 각각 세고 작은 값으로 완전한 사이클 계산
- 평영·접영: 좌우 동시 이벤트를 시간 허용 범위 안에서 하나로 병합
- 자유형·배영 킥: 양 발목 상대 신호의 양·음 극값을 각각 유효 비트 후보로 사용
- 평영: 양 발목 간격의 주기적 최대값 사용
- 접영: 양 발목 평균 움직임의 주기적 최대값 사용
- 발목 가시성이 기준보다 낮으면 킥 수를 숨기고 `legs_not_visible` 반환
- 전체 처리 프레임 중 한 선수의 연속 관측률이 60% 미만이면 짧게 조각난 트랙으로
  보고 팔·발 카운트를 모두 숨김

현재 결과는 포즈 좌표와 적응형 피크 검출을 사용한 기준선이다. 수동 검수 이벤트가
쌓이면 동일한 입출력 계약을 유지하면서 TCN/1D CNN 이벤트 탐지 모델로 교체한다.

## 2026-08-26 실제 영상 스모크 관찰

저장소의 640×360 자유형 전체 경기 영상 한 건을 권리·정답 검증이 아닌 로컬 동작
확인용으로만 실행했다. 선택 프레임에서 겹침 타일 방식은 포즈 후보 5개를 반환해
전체 프레임 방식보다 여러 선수를 찾는 효과는 확인했다. 그러나 전체 구간에서는
물보라·작은 신체 크기·장면 변화 때문에 다수 트랙이 짧게 분절됐고, 한 선수만
카운팅 최소 프레임을 안정적으로 충족했다. 정답 라벨이 없으므로 이 실행은 정확도
수치가 아니며 공개 가능 판정에도 사용하지 않는다.

다음 실험의 우선순위는 자동 수영장·레인 영역 보정보다 먼저 고정된 경기 구간과
수동 pool ROI를 사용해 MediaPipe Lite와 수영 영상용 포즈 모델 후보의 포즈 검출률,
트랙 ID 전환 횟수, 연속 관측률을 같은 프레임에서 비교하는 것이다.

## 공개 전 필수 통과 조건

1. 권리 확인된 영상만 학습·평가에 포함
2. 같은 원본·채널·선수·경기·재업로드를 데이터 분할 사이에 섞지 않음
3. 스트로크 10회당 평균 절대 오차 1회 이하
4. 평가 영상 90% 이상에서 정답 대비 ±1 스트로크
5. 발차기는 커버리지와 오차를 함께 공개하고 가림 영상에서 결과를 보류
6. 장면 전환·리플레이·배속 구간 자동 차단
7. 실제 대회 영상의 레인별 ID 유지율과 ID 전환 횟수 측정
8. 별도 비동기 Worker, 영상 보관·삭제 정책, 소유권 검증, E2E 완료

이 조건 전에는 README·릴리즈 노트·Notion 서비스 설명서에서 공개 기능으로
표현하지 않는다.
