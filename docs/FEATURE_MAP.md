# SwimMate 기능 지도

> 코드 기준일: 2026-08-21 · 기준 브랜치: `main`

이 문서는 “어떤 사용자가 어느 화면에서 무엇을 하고, 어떤 API와 데이터로 이어지는가”를 빠르게 확인하기 위한 지도다. 마케팅 설명이 아니라 현재 `api/main.py`, 등록 라우터, 프론트엔드 페이지와 QA 코드를 대조한 결과를 적는다.

## 역할과 접근 경계

| 역할 | 진입 방식 | 볼 수 있는 범위 |
| --- | --- | --- |
| 비회원 | `/login`, 공개 정보 페이지 | 로그인·가입·체험 진입, 훈련 플랜 둘러보기, 드릴·부상·장비·FAQ·릴리즈 노트·피드백, 사용자가 공개한 결과 카드·클럽 초대 |
| 비회원 체험 사용자 | 로그인 화면의 `/auth/demo` | 격리된 샘플 계정으로 `/landing`, 대시보드, 일지, 리포트, 뱃지 등 회원 흐름 체험 |
| 일반 회원 | 로컬 또는 Google/Kakao 로그인 | 본인 훈련·커뮤니티·코치 연동, 참여한 클럽·반 데이터 |
| 등록 코치 | 일반 회원이 코치 프로필 등록 | 일반 회원 기능 + 연동 학생 조회·피드백·강습 운영·클럽·반 생성·Jira 운영판 |
| 선택 인증 코치 | 자격 정보 제출 후 관리자 확인 | 기능 권한은 등록 코치와 같고 학생 화면에 본인 확인 배지 표시 |
| 슈퍼 관리자 | `role='admin'` 또는 `ADMIN_ID` 호환 계정 | 운영 집계, 사용자·코치·피드백·일반 사용자 운영 로그·QA 검증 로그 |

코치 권한은 관리자 승인 대기 상태와 무관하다. 등록 즉시 코치 코드가 발급되며, 학생이 직접 코드를 입력해 활성 관계를 만들어야 학생 기록에 접근할 수 있다.

## 공개·회원 페이지

| 페이지 | 접근 | 주요 기능 | 주요 API·데이터 | 상태 |
| --- | --- | --- | --- | --- |
| `/`, `/app` | 공개 리다이렉트 | 대표 홈 주소 통일 | 307 → `/landing` | 활성 |
| `/landing` | 로그인 | 이번 주 훈련량·주간 목표·최근 기록·다음 추천을 먼저 보여주는 대표 홈. 월간·누적 지표는 펼침 상세로 제공하고, 전체 기능은 네 개 핵심 메뉴·세 개 상세 그룹·검색·모바일 하단 빠른 메뉴로 탐색 | `/auth/me`, `/api/dashboard/summary`, `/weekly`, `/history`, `/training-advisor`, 페이지 조회 로그 | 활성 |
| `/login`, `/register` | 공개 | 로컬 로그인·가입, 아이디·비밀번호 입력 후 Enter 또는 버튼으로 동일한 폼 제출, Google/Kakao OAuth, 비회원 체험 | `/auth/login`, `/auth/register`, `/auth/demo` | 활성 |
| `/tutorial` | 공개 | 5개 목적별 사용 가이드와 빠른 기능 찾기를 제공하는 짧은 허브 | 정적 콘텐츠, `tutorial.html` | 활성 |
| `/tutorial/{personal|record|data|coach|help}` | 공개 | 개인 훈련, 기록·AI 스크린샷, 성장 데이터, 코치·클럽, 정보·도움을 각각 실제 화면·순서·주의사항으로 분리 안내. 공통 서비스 사이드바와 상세 가이드 탭을 함께 유지 | `tutorial_*.html`, `frontend/static/tutorial/*`, `tutorial-guide.css/js`, `service-nav.js` | 활성 |
| `/privacy`, `/terms` | 공개 | 현재 데이터 처리와 이용 조건 | 정적 문서 | 활성 |
| `/plan` | 공개 조회, 저장은 로그인 | 추천·빠른 생성·직접 구성·내 플랜 네 모드, 목적·풀 길이·레벨·사이클·드릴/대시 플랜, 접힌 세션 상세, 템플릿·즐겨찾기·공유 | `/api/plans`, `/api/training-log/from-plan` | 활성 |
| `/drill` | 공개 | 자유형·배영·평영·접영별 드릴 검색, 필요할 때 펼치는 목적·난이도·25m/50m 필터와 카드별 상세 방법 | 정적 큐레이션·클라이언트 필터 | 활성 |
| `/injury` | 공개 | 수영 전 상태 체크, 공통 부하 관리, 부위별 예방 원칙, 중단·진료 기준과 공신력 출처 | 정적 콘텐츠·클라이언트 상태 체크 | 활성 |
| `/equipment` | 공개 | 보조 장비 사용법, 목적별 수영복 구매 기준, 5개 브랜드 성인 공식 사이즈표 cm/inch 전환, 신체 입력 단위·반대 단위 즉시 환산·cm 내부 정규화, 공식표·현재 라벨·구매 예정 국가 구분, 일반 훈련복 참고 비교와 구매 비보장·테크수트 안전 경계 | 정적 콘텐츠·`swimwear-sizing.js` 클라이언트 비교 | 활성 |
| `/faq` | 공개 | 검색·카테고리·아코디언 | 정적 콘텐츠 | 활성 |
| `/feedback` | 공개 | 서비스 피드백 제출 | `/api/feedback` | 활성 |
| `/changelog` | 공개 | Notion 공개 릴리즈 노트 표시 | `/api/changelog` | `NOTION_TOKEN` 필요, 없으면 503 |
| `/result/{token}` | 공개 | 작성자가 선택적으로 공개한 월간 합계·영법 분포·PB 결과 카드, 1080×1350 PNG 저장·Web Share·링크 복사. 닉네임은 선택 공개이고 위치·심박·메모·원본 스크린샷은 제외 | `/api/promotion/public/results/{token}`, `promotion_result_shares` 스냅샷 | 180일 만료·작성자 즉시 종료 |
| `/club/{token}` | 공개 | 클럽 공동 목표 거리·기간·진행률·선택 회원 수와 반 참여 코드·QR. 회원이 직접 동의한 거리만 익명 합산하고 이름·개인 일지 상세는 비공개 | `/api/promotion/public/clubs/{token}`, `/qr.svg`, `club_promotion_campaigns` | 운영자가 공개한 동안 활성 |
| `/dashboard` | 로그인 | 요약, 주간 목표, 필요할 때 펼치는 준비도와 추천 근거, 개인 수준·목표·선호 풀 기반 규칙 훈련 어드바이저, 최근 기록 | `/api/dashboard/*`, `training_logs`, `training_readiness`, `customers` | 활성 |
| `/my-data` | 로그인 | 평생 누적, 최근 90일 비교, 12개월 추이, 영법·풀 분포, 기록 습관, 테스트 시도·코스별 PB, 규칙 기반 인사이트 | `/api/account/insights`, `training_logs`, `training_log_sets`, `plan_completions`, `swim_test_results` | 활성 |
| `/training-log` | 로그인 | 상단 기록 추가, 일지 CRUD, 세트별 계획·수행·사이클, 월간 목표·통계·캘린더·연속 출석, 테스트 세트·코스별 PB, 최근 기록 복사. 가져오기 메뉴에서 Apple Fitness 스크린샷 최대 5장 확인 등록을 제공하고 워치 직접 동기화는 `준비 중` 비활성 상태 | `/api/training-log/*`, `/api/training-log/screenshot/*`, `/api/training-log/{id}/sets`, `/api/benchmarks` | 일지·스크린샷 확인 등록 활성, 직접 동기화·전체 파일 UI 비활성 |
| `/workout?log={id}` | 로그인 | 풀사이드 현재·다음 세트, 출발 사이클/스톱워치, 반복 완료, RPE·상태·메모, 화면 켜짐 유지 | `GET /api/training-log/{id}/sets`, `PATCH /api/training-log/{id}/sets/{set_id}` | 활성 |
| `/report` | 로그인 | 한 방향으로 읽는 요약 지표·성장률·수행률·차트 흐름에서 월간 거리·횟수·평균·시간·칼로리, 영법·주차·요일, 플랜·세트 수행률, PB와 선택형 결과 카드 관리 | `/api/report/monthly`, `/heatmap`, `/api/promotion/result-shares/*`, `swim_test_results` | 활성 |
| `/badges` | 로그인 | 단계형 뱃지, 다음 목표, 뱃지 여정 | `/api/badges`, 훈련 일지 집계 | 활성 |
| `/challenge` | 로그인 | 거리·연속 출석 챌린지, 참가·탈퇴·랭킹 | `/api/challenge/*` | 활성 |
| `/community` | 로그인 | 글·댓글·좋아요·북마크·태그·멘션·신고·이미지 | `/api/community/*`, `/api/notifications/*` | 활성, 이미지 저장소 없으면 텍스트 중심 |
| `/coach` | 로그인 | 아래 코치·학생 기능을 역할별로 표시 | `/api/coach/*`, `/api/jira/*` | 활성 |
| `/clubs` | 로그인 | 반 코드 참여, 내 클럽·반, 역할 관리, 반 일정·출석·클럽/반 공지와 읽음, 코치용 기간별 수행·출석 분석, 회원별 익명 거리 합산 동의, 운영자용 공개 공동 목표·반 초대·QR 캠페인 | `/api/clubs/*`, `/api/promotion/clubs/{id}/campaign`, `/campaign/consent`, 클럽 운영·캠페인 테이블 | 활성 |
| `/chat` | 로그인 | 검수된 수영 지식·공식 출처·관련 가이드를 연결한 Gemini AI 코치. 명시적 맞춤 질문에만 최소화한 본인 훈련 요약을 반영하고 반영 여부를 답변 아래 표시 | `/api/chat/send`, `/history`, `/context-preview`, `services/swimming_knowledge.py`, `services/chat_personalization.py` | `GEMINI_API_KEY` 필요. 지식·개인화 문맥 미리보기는 Gemini를 호출하지 않음 |
| `/pool` | 로그인 | 카카오맵 수영장 검색·즐겨찾기 | `/api/pool/*`, Kakao Maps SDK | Kakao 키 필요 |
| `/glossary` | 로그인 | 수영 용어 검색과 외부 참고 영상 | 정적 콘텐츠 | 활성 |
| `/videos` | 로그인 | 영법·주제별 외부 영상 큐레이션 | 정적 콘텐츠 | 활성 |
| `/profile` | 로그인 | 계정·이메일, 현재 맞춤 훈련 설정, 닉네임·비밀번호 변경, 내 데이터 대시보드 진입, JSON 데이터 내보내기, 모든 기기 로그아웃, 재인증 회원 탈퇴 | `/auth/me`, `/api/account/*`, `DELETE /auth/me` | 활성 |
| `/onboarding` | 로그인 | 수준·목표·주간 횟수·25m/50m 선호 풀 최초 설정 | `GET/PUT /auth/onboarding`, `customers` | 활성 |
| `/onboarding?mode=edit` | 로그인 | 기존 맞춤 훈련 설정을 불러와 수정하고 프로필로 복귀 | `GET/PUT /auth/onboarding`, `/auth/me` | 활성 |
| `/admin` | 슈퍼 관리자 | 사용자·일반/QA 운영 로그 분리·피드백·훈련 상태·30일 AI 스크린샷 등록·코치 운영 | `/api/admin/*`, `/api/feedback` | 활성 |

주요 기능 페이지는 데스크톱에서 화면 폭에 따라 232~268px로 조정되는 공통 고정 사이드바를 유지한다. 사이드바에는 오늘·기록·플랜·성장 네 개 핵심 메뉴만 항상 노출하고, 분석·코치/커뮤니티·정보/도구는 접힌 그룹으로 제공한다. 기능 검색은 그룹과 무관하게 전체 메뉴를 찾고, 1,100px 이하에서는 하단 빠른 메뉴와 더보기 드로어를 사용한다. 사이드바와 별개로 서비스·온보딩·관리자·개인정보·약관 화면에는 동일한 공통 상단 헤더를 적용해 왼쪽 홈은 `/landing`, 오른쪽은 프로필 수정·로그아웃·테마 변경 순서를 유지한다. 공통 헤더는 본문과 함께 스크롤되고, 사이드바만 남은 헤더 높이에 맞춰 위로 이동한다. 로그인·회원가입·닉네임 설정처럼 인증을 시작하거나 완료하는 화면만 이 인증형 헤더에서 제외한다.

공통 화면 스타일은 그리드·flex 자식의 최소 폭을 0으로 정규화하고 제목·설명·긴 문자열의 안전한 줄바꿈을 적용한다. 서비스·데이터·운영 페이지의 핵심 본문은 사이드바를 제외한 작업 영역 안에서 최대 1,280px로 중앙 정렬하고, 더 작은 화면에서는 사용 가능한 폭에 맞춰 유동적으로 축소한다. 월간 성장 리포트는 1,180px 이하에서 본문과 지표를 1열로 바꿔 숫자·단위·연속 출석 문구가 세로 한 글자 폭으로 눌리지 않게 한다. 자동 UI QA는 전체 경로를 2,560px부터 390px까지 일곱 해상도에서 검사한다.

## 개인 훈련 데이터 흐름

1. 로그인 후 `/landing`의 간결한 개인 훈련 홈으로 이동한다. 이번 주·이번 달·누적 훈련량과 최근 기록, 다음 추천을 먼저 확인하고 다른 기능은 카테고리형 사이드바에서 선택한다. 기능 화면으로 이동한 뒤에도 같은 메뉴를 사용해 다른 서비스로 바로 전환한다. 설정이 미완료라면 랜딩 안내에서 수준·목표·주간 횟수·선호 풀을 저장하고, 완료된 설정은 프로필에서 다시 수정한다.
2. 대시보드 어드바이저와 플랜 생성 폼이 같은 설정을 추천 거리·목표 세트·풀 길이·난이도 기본값으로 사용한다.
3. 사용자가 대시보드 준비도를 저장하거나 플랜을 선택한다.
4. 일지를 직접 작성하거나 플랜에서 세트가 포함된 일지를 만든다. 또는 사용자가 Apple Fitness 수영 요약 이미지를 최대 5장까지 선택해 한 장씩 AI가 읽은 값을 운동별로 확인·수정하고 일지와 영법별 완료 세트로 확정한다.
5. 출발 사이클 또는 스톱워치로 반복을 진행하고 실제 횟수·거리·평균 사이클·RPE·상태·메모를 한 세트씩 저장한다.
6. 일지의 총거리·시간은 `training_logs`, 반복·거리·사이클·완료량은 소유권이 같은 `training_log_sets`에 순서대로 저장된다.
7. 단일 세트 저장과 같은 트랜잭션에서 실제 완료 거리 합계가 일지 총거리에 반영된다.
8. `training_logs`가 대시보드, 월간 리포트, 뱃지와 챌린지의 공통 기준 데이터가 되고, 세트 데이터는 일지와 리포트의 수행률을 보완한다.
9. 플랜에서 생성한 일지는 `plan_completions`와 연결되어 리포트의 플랜 완료 세션·거리·사이클 포함률에 반영된다.
10. 월간 목표는 `training_goals`에 저장되고 일지와 리포트에서 같은 고객 ID로 집계된다.
11. 테스트 세트는 `swim_test_results`에 0.01초 단위로 저장되며, 영법·거리·풀 길이가 같은 기록끼리만 PB를 비교한다.
12. 테스트 기록을 일지에 연결할 때는 같은 소유자·날짜·풀 길이를 검사하고, 월간 리포트는 해당 월 시도·신규 PB와 전체 현재 최고기록을 함께 집계한다.
13. `/my-data`는 같은 고객 ID의 전체 기록을 평생 누적·최근 90일·12개월·영법·풀·기록 습관·코스별 PB로 다시 읽어 장기 변화와 다음 행동을 설명한다. JSON 내보내기는 백업·이동용 원본 역할을 유지한다.
14. 월간 결과 공유는 현재 집계에서 공개 허용 필드만 별도 스냅샷으로 복사한다. 이후 원본 일지 수정과 무관하게 생성 당시 합계를 유지하며, 닉네임 공개 선택·180일 만료·작성자 종료를 적용한다.

워치 직접 동기화와 건강 전체 파일 가져오기는 현재 공개 화면에서 비활성 상태다. 이와 별도로 사용자가 직접 선택한 운동 스크린샷 확인 등록은 활성 상태다.

- 훈련 일지의 `워치 데이터 가져오기 (준비 중)` 버튼은 `disabled`와 `aria-disabled`를 사용하며 모달 열기 이벤트를 연결하지 않는다.
- `/api/training-log/import/*`와 파서 코드는 향후 재검증을 위해 남아 있지만 현재 사용자 기능으로 소개하지 않는다.
- Apple Watch·Galaxy Watch 직접 동기화와 Health Auto Export·Apple 건강·Samsung Health 파일 가져오기를 현재 제공 기능으로 약속하지 않는다.
- 브라우저는 최대 5장을 큐에 담되 `/api/training-log/screenshot/preview`를 순차 호출한다. 각 호출은 PNG·JPEG·WEBP·HEIC·HEIF 서명을 확인한 선택 이미지 한 장만 Gemini에 전달하고, 서버·DB에는 원본 이미지를 저장하지 않는다.
- AI 결과는 즉시 기록하지 않는다. 고객별 20분 확인 토큰, 값 범위·영법 합계·랩×풀 길이 경고와 사용자 수정 단계를 거친 `/confirm` 요청만 `training_logs`, `training_log_sets`, `wearable_workouts`에 구조화 값을 저장한다.
- 1차 인식 대상은 Apple Fitness 수영 요약이며 공급자 필드는 Samsung Health 확장과 분리돼 있다.

## 코치·수강생 기능

### 클럽·반 그룹 구조

1. 등록 코치가 클럽을 만들면 클럽 소유자 멤버십이 함께 생성된다.
2. 클럽 소유자나 클럽 코치는 수준·목표·풀 길이·정원을 정해 반을 만들고 `LANE-XXXXXX` 참여 코드를 받는다.
3. 학생이 반 코드를 입력하면 클럽 `member`와 반 `student` 멤버십이 같은 트랜잭션에서 생성된다.
4. 클럽 역할(`owner`, `coach`, `assistant`, `member`)과 반 역할(`coach`, `assistant`, `student`)은 범위별로 분리한다.
5. 코치·보조 코치 역할은 `coaches`에 등록된 회원에게만 부여하며, 반 코치는 클럽 전체가 아닌 지정된 반만 관리할 수 있다.
6. 정원, 담당 코치 강등·이탈, 다른 클럽·반 접근을 API에서 다시 검사한다.
7. 클럽 운영자 또는 해당 반 코치가 수업 일정을 등록하고 학생 명단에 출석 상태를 기록한다.
8. 클럽 전체·반 공지는 대상 회원에게 알림으로 전달되며 사용자별 읽음 상태를 따로 저장한다.
9. 반 운영자는 확인된 출석률과 기록 완료율을 분리해 보고 주간 추이와 학생별 확인 필요 신호를 확인한다.
10. 개인 훈련 횟수·거리는 반 멤버십이 아니라 현재 `coach_students` 활성 관계가 별도로 있는 담당 코치에게만 표시한다.
11. 클럽 소유자·코치는 클럽 또는 한 반을 대상으로 공동 거리 목표와 기간을 공개하고 QR을 배포한다. 공개 응답은 익명 합산 거리와 선택적 회원 수만 제공하며 개인 회원 목록·일지를 포함하지 않는다. 개인 일지 거리는 각 회원이 직접 공개 합산에 동의한 경우에만 포함한다.

### 학생이 보는 기능

- 코치 코드 입력, 새 코드로 관계 교체, 직접 연동 해제
- 코치 프로필과 선택 인증 상태 확인
- 코치 피드백·개별 플랜·강습 일지·강습표·일정표 수신
- 참여한 반의 일정·공지를 확인하고 본인 출석 상태만 조회
- 오늘 자유수영 내용을 코치에게 공유

### 코치가 보는 기능

- 등록 즉시 `SWIM-XXXX` 코치 코드 발급·복사·기기 공유
- 선택 자격정보 제출과 상태 확인
- 연동 학생 목록, 최근 일지, 자유수영 공유, 피드백과 개별 플랜
- 전체 학생 대상 강습 일지
- AI 또는 규칙 템플릿 기반 단체 강습표·과정 일정표 생성
- 코치 검토·수정 후 전체 또는 선택 학생 배포
- 학생 실명 대신 `S1`, `S2`를 Gemini에 보내는 익명 수업 브리핑
- 최근 30일 훈련·준비도 신호 기반 크루 운영판
- 코칭 과제 로컬 저장, Jira 동기화·분석·완료 처리
- 담당 반 일정·출석·공지를 운영하고 공지 읽음 인원 확인
- 담당 반의 주간 출석 추이와 학생별 수행 신호 확인. 개인 훈련량은 코치 코드 연동 학생만 조회

Jira가 설정되지 않거나 호출에 실패해도 코칭 과제는 SwimMate DB에 먼저 남는다. Jira 웹훅은 `JIRA_WEBHOOK_SECRET` HMAC 서명을 검증한 뒤 상태를 갱신한다.

## 관리자 기능

모든 주요 목록은 20·50·100개 표시와 번호 페이지 이동을 지원한다. 사용자·코치·일반 운영 로그·QA 검증 로그·피드백은 전체 또는 필드별 검색을 서버 페이지네이션과 함께 적용한다. 관리자 기능 8개는 데스크톱의 고정 사이드 메뉴와 모바일의 `운영 메뉴` 드로어에서 전환한다. 관리자 전용 메뉴에는 일반 사용자 대시보드 링크를 섞지 않되, 전 화면 공통 헤더의 `SwimMate 홈`으로 대표 홈 `/landing`에 돌아갈 수 있고 프로필·로그아웃·테마도 같은 위치에서 사용한다. GitHub Actions는 관리자 권한으로 일반·학생 QA 계정을 DB에 표식하고, `/api/admin/logs?account_scope=regular|qa`가 과거·현재 활동을 계정·자동화 표식·동일 세션 근거로 분리한다.

| 탭·영역 | 확인 항목 |
| --- | --- |
| 운영 요약 | 7/30/90일 페이지 조회·추정 순 방문자·로그인 회원·신규 가입 추이 그래프, 가입 방식 분포 |
| 사용자 | ID·이름·이메일·아이디·닉네임·가입 방식·상태 검색, 전체/일반/확정 QA/미분류 후보 필터, 최근 로그인·최근 활동·이벤트/훈련 수·판정 근거, QA/일반 명시적 전환, 페이지 크기·페이지 이동 |
| 코치 운영 | 이름·아이디·이메일·전문 분야·경력·자격 정보 검색, 등록·미인증·인증 대기·완료·반려, 최근 AI 문서·배포 |
| 훈련 운영 | 30일 일지·활성 사용자·거리·평균, 목표·플랜 완료, 준비도, 테스트 참여 사용자·테스트 기록·PB, 영법·풀 길이, 활성 클럽·반, 30일 반 일정·출석률·공지 |
| 피드백 | 유형 필터, 작성자·페이지·제목·내용·이메일 검색, 작성자 닉네임·계정 정보와 내용 |
| 운영 로그 | 확정 QA 계정·자동화 표식·근접 익명 QA 세션을 제외한 일반 사용자·비회원 이벤트, 페이지 조회 포함 유형 필터, 아이디·경로·동작·HTTP 방식·IP 검색, 페이지 크기·페이지 이동 |
| QA 검증 로그 | DB 확정 QA 계정과 자동화 표식·동일 IP+브라우저+15분 범위의 연계 익명 세션, 등록 계정·30일 이벤트·페이지 조회·최근 활동 집계, 같은 검색·필터·페이지 이동 |

관리자 QA는 운영 데이터 변경을 피하기 위해 조회·탭·필터 중심으로 실행한다.

## AI 코치 지식·개인화 흐름

1. 최근 사용자 발화 최대 3개를 묶어 후속 질문의 실제 주제를 만든다.
2. 키워드 점수로 영법·사이클·풀 길이·장비·안전·위생·규정·회복 중 관련된 검수 지식 최대 3개를 선택한다.
3. `내 기록`, `최근 훈련`, `맞춤 추천`처럼 명시적인 개인화 의도가 있을 때만 로그인 사용자의 훈련 설정·최근 일지 수치·주간/월간 요약·목표·준비도·PB·플랜/세트 수행 요약을 읽는다.
4. 이름·이메일·사용자명·일지 메모·준비도 자유 메모를 제외한 문맥을 Gemini에 전달한다.
5. 답변에는 선택된 지식 분야, 공식 출처, 관련 내부 가이드와 개인 기록 반영 여부를 함께 반환한다.
6. 경기 규정은 `최신 확인 필요`로 표시하고 World Aquatics 원문과 참가 대회 요강을 다시 확인하도록 안내한다.

## AI와 규칙 기반 기능 구분

| 기능 | 방식 |
| --- | --- |
| AI 코치 대화 | 결정적 지식 검색·명시적 본인 데이터 최소화 후 Google Gemini가 자연어 답변 생성 |
| 코치 단체 강습표·과정 일정표 | Gemini 구조화 출력, 실패 시 규칙 템플릿 |
| 코치 수업 브리핑 | Gemini 또는 규칙 템플릿, 학생 익명 참조 |
| 준비도 점수 | 수면·피로·근육 상태 가중 규칙 |
| 주간 훈련 어드바이저 | 최근 일지·주간 목표·준비도 분기 규칙 |
| 교정 포인트 플랜 | 사용자가 직접 고른 고민과 세트 태그 매칭 |
| 내 수영 데이터 인사이트 | PostgreSQL 장기 집계와 기록 상태별 설명 가능한 분기 규칙 |
| 월간 리포트·뱃지·챌린지 | PostgreSQL 집계 규칙 |

## 공개 비활성·레거시

다음은 현재 공개 기능으로 설명하면 안 된다.

| 항목 | 현재 동작 |
| --- | --- |
| 영상 업로드 영법 분석 | 라우터 미등록 |
| `/meta`, `/upload`, `/viewer` | `/landing`으로 이동 |
| 과거 `/share/*` 영상 분석 공유 | 홈 이동 또는 API 410 |
| `/api/video-stream/*` | 410 |
| `analysis/`, worker, MediaPipe/OpenCV | 저장소의 실험·레거시 코드, 공개 제품 경로와 분리 |
| `analysis_v2/` 다중 선수 스트로크·킥 카운터 | 오프라인 기준선과 합성 회귀 테스트만 구성. 실제 대회 영상 정확도·권리·비동기 인프라 검증 전 공개 제품 경로와 분리 |
| Apple Watch·Galaxy Watch 직접 동기화 | 미지원 |
| 건강 앱 전체 내보내기 파일 가져오기 | 공개 UI 비활성, 파서·API 코드는 재검증 전까지 비노출 |
| 운동 스크린샷 AI 확인 등록 | Apple Fitness 수영 요약 1차 지원. 최대 5장 다중 선택·순차 일시 분석, 원본 비저장, 운동별 사용자 확인 후 일지·영법 세트 저장 |

## 관련 문서

- [README](../README.md)
- [기술 구조](./ARCHITECTURE.md)
- [배포 가이드](./DEPLOYMENT.md)
- [품질 검증 게이트](./QUALITY_GATE.md)
- [Postman API 스모크 가이드](../tests/postman/README.md)
- [기능 체크리스트](../FEATURE_CHECKLIST.md)
